# 실제 모델을 붙일 때 채운다. 지금은 비어 있다.
# 예정: openai-whisper / faster-whisper, TTS 클라이언트, LLM 클라이언트
# 실제 모델. openai-whisper와 faster-whisper 중 비교 후 faster-whisper로 확정.
# (같은 large-v3 가중치, CTranslate2 기반이라 더 빠르고 한국어 필러워드 검증 결과도 동일)

faster-whisper==1.2.1

# 시선 분석(infra/gaze_analysis/gaze_pipeline.py)이 영상을 내려받을 때 쓴다.
requests==2.32.5

# torch · opencv-python · l2cs는 여기 넣지 않는다. torch는 서버의 CUDA 버전에
# 맞춰 골라 깔아야 해서 GPU 서버에서 따로 설치한다.
