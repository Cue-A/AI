# 실제 모델을 붙일 때 채운다. 지금은 비어 있다.
# 예정: openai-whisper / faster-whisper, TTS 클라이언트, LLM 클라이언트
# 실제 모델. openai-whisper와 faster-whisper 중 비교 후 faster-whisper로 확정.
# (같은 large-v3 가중치, CTranslate2 기반이라 더 빠르고 한국어 필러워드 검증 결과도 동일)

faster-whisper==1.2.1

# 시선 분석 (L2CS-Net). torch는 CUDA 12.8 드라이버(Yennefer 서버) 기준 인덱스로 설치.
# 다른 드라이버 버전 서버에 배포할 땐 --extra-index-url만 맞는 cuXXX로 바꾸면 됨.
# GPU 워커는 --pool=solo 필수 (기본 prefork는 fork+CUDA 충돌로 교착 - infra/Dockerfile 참고)
--extra-index-url https://download.pytorch.org/whl/cu128
torch
torchvision
opencv-python-headless
git+https://github.com/Ahmednull/L2CS-Net.git@main
